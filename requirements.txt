# HerEducation - Vollständige Requirements für Streamlit Cloud
streamlit==1.45.0
pydeck==0.8.0
pandas>=2.0.0
numpy==2.4.6  # wie _bench/.venv (MESS1–3); braucht Python >= 3.11
plotly>=5.14.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# NLP & Machine Learning
# Exakt gepinnt (25.09.2026): Versionen aus der MESS1-Messung auf dem Mac (_bench/.venv).
# Ungepinnt zieht jeder neue Cloud-Build die neueste Version; mit transformers 5.17
# wirkt return_all_scores nicht mehr (Emotionsanalyse falsch/leer, behoben in
# models/pipeline_adapters.py). Cloud-App laeuft mit Python 3.11 (geprueft 25.09.2026).
torch==2.14.0
transformers==5.17.0
sentence-transformers==6.1.0
bertopic==0.17.4
hdbscan==0.8.44
umap-learn==0.5.12
nltk>=3.8.0
langdetect>=1.0.9

# Data Processing
requests>=2.30.0
PyYAML>=6.0.0

# KRITISCH für YouTube_Analyzer - ohne diese funktioniert die App nicht
chardet>=5.0.0
youtube-comment-downloader>=0.1.70
yt-dlp>=2023.7.6

# Text Analysis
wordcloud>=1.9.0

# Optional: SpaCy
spacy>=3.6.0

# NICHT für Streamlit Cloud (zu groß/problematisch):
# openai-whisper>=20231117